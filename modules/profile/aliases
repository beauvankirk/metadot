# Generic aliases
alias kedpmc="kedpm -c"
alias gvim="vim.gtk -g &> /dev/null"
alias drush="php -d error_reporting=E_ALL^E_NOTICE `which drush`"
alias news="newsbeuter #; newsbeuter -X"
alias music="ncmpcpp"

# Editor, avoiding "Avoiding protocol specified" error
alias vim="vim -X"
alias vi="vim -X"

# Development
alias dquilt="quilt --quiltrc=${HOME}/.quiltrc-dpkg"

# Homecrypt aliases
#alias logout="cd /home ; sudo /usr/bin/homecrypt off `whoami`; logout"
#alias exit="cd /home ; sudo /usr/bin/homecrypt off `whoami`; exit"
